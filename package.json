{
  "name": "lander",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "npx vite preview"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
